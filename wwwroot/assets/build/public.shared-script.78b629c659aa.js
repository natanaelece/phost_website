(() => {
  'use strict';

  const handlers = Object.freeze({
    h001: function (event) { doLogin() },
    h002: function (event) { doLogout() },
    h003: function (event) { refresh() },
    h004: function (event) { openAdUserModal() },
    h005: function (event) { openAdGroupModal() },
    h006: function (event) { openAdComputerModal() },
    h007: function (event) { loadAd() },
    h008: function (event) { setAdFilter('users',this) },
    h009: function (event) { setAdFilter('website',this) },
    h010: function (event) { setAdFilter('expired',this) },
    h011: function (event) { setAdFilter('groups',this) },
    h012: function (event) { setAdFilter('computers',this) },
    h013: function (event) { go('usuarios') },
    h014: function (event) { handlePeriodChange() },
    h015: function (event) { reloadDashViews() },
    h016: function (event) { go('crm') },
    h017: function (event) { go('pedidos') },
    h018: function (event) { go('dashboard') },
    h019: function (event) { openNewWhatsAppTemplate() },
    h020: function (event) { resetWhatsAppTemplate() },
    h021: function (event) { saveWhatsAppTemplate() },
    h022: function (event) { renderWhatsAppPreview(this.value) },
    h023: function (event) { closeModals() },
    h024: function (event) { createWhatsAppTemplate() },
    h025: function (event) { confirmCancelOrder(false) },
    h026: function (event) { confirmCancelOrder(true) },
    h027: function (event) { mascaraTelefone(this) },
    h028: function (event) { submitLocalUser() },
    h029: function (event) { submitAdUser() },
    h030: function (event) { submitAdEdit() },
    h031: function (event) { submitAdPassword() },
    h032: function (event) { openAdLinkDropdown() },
    h033: function (event) { renderAdLinkDropdown(true) },
    h034: function (event) { toggleAdLinkDropdown() },
    h035: function (event) { submitAdLink() },
    h036: function (event) { renderAdComputerChecks() },
    h037: function (event) { saveAdAccesses() },
    h038: function (event) { resolveAdminConfirm(false) },
    h039: function (event) { resolveAdminConfirm(true) },
    h040: function (event) { cancelManualGroupSelection() },
    h041: function (event) { confirmManualGroupSelection() },
    h042: function (event) { submitAdGroup() },
    h043: function (event) { this.value=this.value.toUpperCase().replace(/[^A-Z0-9-]/g,'') },
    h044: function (event) { submitAdComputer() },
    h045: function (event) { saveAdComputerGroups() },
    h046: function (event) { syncManualOrderPeriod() },
    h047: function (event) { updateManualOrderSuggestedPrice() },
    h048: function (event) { saveManualOrder() },
    h049: function (event) { submitDuplicate() },
    h050: function (event) { openManualOrderModal() },
    h051: function (event) { setFilter('all',this) },
    h052: function (event) { setFilter('active_license',this) },
    h053: function (event) { setFilter('expired_license',this) },
    h054: function (event) { setFilter('pago',this) },
    h055: function (event) { setFilter('pendente',this) },
    h056: function (event) { setFilter('cancelado',this) },
    h057: function (event) { setFilter('expirado',this) },
    h058: function (event) { handleTrialSearch(this.value) },
    h059: function (event) { setTrialFilter('all',this) },
    h060: function (event) { setTrialFilter('never_requested',this) },
    h061: function (event) { setTrialFilter('not_used',this) },
    h062: function (event) { setTrialFilter('solicitado',this) },
    h063: function (event) { setTrialFilter('liberado',this) },
    h064: function (event) { setTrialFilter('utilizado',this) },
    h065: function (event) { setTrialFilter('recusado',this) },
    h066: function (event) { setTrialFilter('cancelado',this) },
    h067: function (event) { openUserModal() },
    h068: function (event) { handleSearch(this.value) },
    h069: function (event) { abrirModalAuth('login') },
    h070: function (event) { document.getElementById('mobileMenuIndex').classList.toggle('hidden') },
    h071: function (event) { fecharMenuMobile() },
    h072: function (event) { abrirModalAuth('login'); document.getElementById('mobileMenuIndex').classList.add('hidden') },
    h073: function (event) { solicitarTesteGratis() },
    h074: function (event) { if(event.target === this) fecharModalAuth() },
    h075: function (event) { fecharModalAuth() },
    h076: function (event) { switchTab('login') },
    h077: function (event) { switchTab('register') },
    h078: function (event) { limparErro('logEmail', 'logEmailError') },
    h079: function (event) { limparErro('logPass', 'logPassError') },
    h080: function (event) { switchTab('recover') },
    h081: function (event) { limparErro('regName', 'nameErrorMsg') },
    h082: function (event) { mascaraTelefone(this); limparErro('regWhatsapp', 'waErrorMsg') },
    h083: function (event) { limparErro('regEmail', 'emailErrorMsg') },
    h084: function (event) { limparErro('regPass', 'passErrorMsg') },
    h085: function (event) { handleReferralInput() },
    h086: function (event) { limparErro('regPrivacy', 'privacyErrorMsg') },
    h087: function (event) { previousRegisterStep() },
    h088: function (event) { nextRegisterStep() },
    h089: function (event) { return handleRecover(event) },
    h090: function (event) { limparErro('recEmail', 'recEmailError') },
    h091: function (event) { closeRegisterSuccess() },
    h092: function (event) { abrirAjuda('como-usar') },
    h093: function (event) { abrirAjuda('faq') },
    h094: function (event) { abrirPerfil() },
    h095: function (event) { logout() },
    h096: function (event) { toggleMobileMenu() },
    h097: function (event) { abrirAjuda('como-usar'); fecharMenuMobile() },
    h098: function (event) { abrirAjuda('faq'); fecharMenuMobile() },
    h099: function (event) { abrirPerfil(); document.getElementById('mobileMenu').classList.add('hidden') },
    h100: function (event) { fecharAjudaAoClicarFora(event) },
    h101: function (event) { fecharAjuda() },
    h102: function (event) { fecharBoasVindasCanal() },
    h103: function (event) { closeFreeTrialIneligibleModal() },
    h104: function (event) { toggleFreeTrial(this) },
    h105: function (event) { requestFreeTrial() },
    h106: function (event) { limparErro('myReferralCode', 'refErrorMsg') },
    h107: function (event) { salvarMeuCodigo() },
    h108: function (event) { cancelarPixPendente() },
    h109: function (event) { retomarPix() },
    h110: function (event) { toggleHistory(this) },
    h111: function (event) { atualizarSlider(this); limparErro('qtdPcs', 'msgPcsAlert'); calcular() },
    h112: function (event) { atualizarSlider(this); limparErro('qtdWyds'); calcular() },
    h113: function (event) { calcular() },
    h114: function (event) { atualizarSlider(this); limparErro('qtdDias', 'msgDiasAlert'); calcular() },
    h115: function (event) { limparErro('wydServerName', 'wydServerErrorMsg') },
    h116: function (event) { validarServidorWyd(true) },
    h117: function (event) { this.value = this.value.replace(/[^0-9]/g, ''); limparErro('anydeskId', 'anydeskErrorMsg') },
    h118: function (event) { aplicarCupom() },
    h119: function (event) { verificarERodarPix() },
    h120: function (event) { fecharPixModal() },
    h121: function (event) { copiarPix() },
    h122: function (event) { window.location.reload() },
    h123: function (event) { fecharAvisoServidor() },
    h124: function (event) { fecharPerfil() },
    h125: function (event) { togglePasswordFields(this) },
    h126: function (event) { limparErro('profPassCurrent', 'profErrorMsg') },
    h127: function (event) { limparErro('profPassNew', 'profErrorMsg') },
    h128: function (event) { limparErro('profPassConfirm', 'profErrorMsg') },
    h129: function (event) { salvarPerfil() },
    h130: function (event) { return handleReset(event) },
    h131: function (event) { limparErroReset('newPass', 'newPassError') },
    h132: function (event) { limparErroReset('confirmPass', 'confirmPassError') },
    h133: function (event) { copiarMeuCodigo() },
    h134: function (event) { removerCupom() }
  });

  const dispatch = (eventType, event) => {
    const selector = `[data-csp-${eventType}]`;
    const origin = event.target instanceof Element ? event.target : null;
    const element = origin?.closest(selector);
    if (!element) return;

    const handler = handlers[element.getAttribute(`data-csp-${eventType}`)];
    if (!handler) return;

    const result = handler.call(element, event);
    if (result === false) {
      event.preventDefault();
      event.stopPropagation();
    }
  };

  document.addEventListener('click', (event) => dispatch('click', event), false);
  document.addEventListener('change', (event) => dispatch('change', event), false);
  document.addEventListener('input', (event) => dispatch('input', event), false);
  document.addEventListener('focus', (event) => dispatch('focus', event), true);
  document.addEventListener('submit', (event) => dispatch('submit', event), false);
  document.addEventListener('blur', (event) => dispatch('blur', event), true);
})();
